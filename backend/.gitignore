# Environment
.env
.env.local
.env.keys
keys.txt
rebuild.log
nohup.out

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Project specific
# indexes/ ARE committed so RAG (book answers) works on Render.
# subjects/ (raw PDFs) stay ignored — only the built indexes are needed at runtime.
subjects/
indexes/*/_partial.pkl

# OS
.DS_Store
Thumbs.db